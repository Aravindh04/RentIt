<?xml version="1.0" encoding="UTF-8"?>
<!--
  Deploy AFTER the main metadata push to remove the Original_Amount__c field from Invoice__c.
  This field was replaced by Discount_Amount__c (now a stored Currency field) and
  Total_Amount__c (updated formula: Amount - Discount + GST).

  Command:
    sf project deploy start --manifest manifest/package.xml \
      --post-destructive-changes manifest/destructiveChangesPost.xml
-->
<Package xmlns="http://soap.sforce.com/2006/04/metadata">
    <types>
        <members>Invoice__c.Original_Amount__c</members>
        <name>CustomField</name>
    </types>
    <version>62.0</version>
</Package>
