<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Total_Amount__c</fullName>
    <description>Net amount payable on this invoice: (Rent Amount minus Discount) plus GST. This is the figure tenants must pay and that credit applications are measured against.</description>
    <formula>(Amount__c - Discount_Amount__c) + IF(ISNULL(GST_Amount__c), 0, GST_Amount__c)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Total Amount (inc GST)</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Currency</type>
</CustomField>
